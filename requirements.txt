# Core
pydantic>=2.6,<3
PyYAML>=6.0
python-dotenv>=1.0

# Data / indicators
pandas>=2.0
numpy>=1.24

# Broker backends
# MetaTrader5 only installs on Windows; keep it optional if deploying elsewhere.
MetaTrader5>=5.0.45; sys_platform == "win32"
ccxt>=4.3

# Testing
pytest>=7.4
pytest-cov>=4.1
